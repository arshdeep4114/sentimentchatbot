Here is a **clean, professional, submission-ready README.md** for your **Sentiment Analysis Chatbot** project.
It includes: setup, installation, technologies used, explanation of sentiment logic, Tier 1 & Tier 2 compliance, model switching option, and usage instructions.

---

# 📌 Sentiment Analysis Chatbot

A conversational AI chatbot with **statement-level** and **conversation-level** sentiment analysis.
Built using **Python + Flask**, integrates **Groq LLM**, **VADER**, **Transformers**, and **TextBlob** for fast & accurate hybrid sentiment scoring.

---

## 🚀 Features

### ✅ Tier 1 – Mandatory

✔ Maintains full conversation history
✔ Generates **overall sentiment** for the entire chat
✔ Detects conversation trend (improving, declining, stable)
✔ Detects mood shifts (significant / moderate / consistent)

### 🏆 Tier 2 – Additional Credit

✔ Performs **sentiment analysis on each user message**
✔ Displays: polarity, label, subjectivity, confidence
✔ Includes mood-shift + average polarity metrics
✔ Optional: Model switching support via backend variable

### 💡 Additional Enhancements

✔ **Hybrid Sentiment Engine** (VADER + Transformers + TextBlob fallback)
✔ **LRU caching** → extremely fast repeated responses
✔ **Single-time model initialization** → high performance
✔ Clean modular code (SentimentAnalyzer + ConversationManager)

---

# 📂 Project Structure

```
project/
│── app.py                 # Main Flask app
│── requirements.txt       # Dependencies
│── static/
│     └── index.html       # Simple frontend UI
│── README.md
│── .env                   # GROQ_API_KEY + model
```

---

# 🔧 Tech Stack

| Component          | Technology                                        |
| ------------------ | ------------------------------------------------- |
| Backend            | Python, Flask, Flask-CORS                         |
| AI Model           | Groq Chat Completions API                         |
| Sentiment Analysis | VADER, Transformers (DistilBERT SST-2), TextBlob  |
| Frontend           | HTML/JS                                           |
| Deployment         | Local / Render / Railway / Vercel (via API proxy) |

---

# 📑 Sentiment Analysis Logic (Important for your submission)

### 1️⃣ **VADER (Primary Fast Pass)**

* Instantly evaluates the message
* If compound score ≥ 0.6 → Positive
* If compound score ≤ −0.6 → Negative
* Used for speed and social-language accuracy (slang, emojis, tone)

### 2️⃣ **Transformers (Accurate Fallback)**

* Uses model: **distilbert-base-uncased-finetuned-sst-2-english**
* Only used when VADER is uncertain (−0.6 < score < 0.6)
* Produces accurate positive/negative classification
* Score processed through a scaled sigmoid → smooth polarity

### 3️⃣ **TextBlob (Final Fallback)**

* Used if VADER & Transformers unavailable
* Provides polarity + subjectivity

### 4️⃣ **Caching (LRU)**

* If the same text appears again → instant cached response
* Makes the chatbot extremely fast in real use

### 5️⃣ **Conversation-Level Sentiment**

Generated by aggregating all per-message polarities:

* **average_polarity**
* polarity range (min → max)
* **trend** (improving / declining / stable)
* **sentiment_shift** (significant / moderate / consistent)
* distribution (positive / negative / neutral)
* overall sentiment label based on average polarity

---

# 🛠 Installation & Setup

## 1. Clone the repo

```bash
git clone <your-repository-url>
cd project
```

## 2. Create & activate a virtual environment (optional but recommended)

```bash
python -m venv venv
venv\Scripts\activate    # Windows
```

## 3. Install dependencies

```bash
pip install -r requirements.txt
```

## 4. Install required NLP corpora

```bash
python -m nltk.downloader vader_lexicon
python -m textblob.download_corpora
```

## 5. Set up your `.env`

Create `.env`:

```
GROQ_API_KEY=your_key_here
DEFAULT_GROQ_MODEL=openai/gpt-oss-20b
```

---

# ▶ Running the Chatbot

Start the server:

```bash
python app.py
```

You will see:

```
🤖 Sentiment Analysis Chatbot
Server: http://localhost:5000
Model: openai/gpt-oss-20b
Enhanced Sentiment Analysis: Active
```

Open your browser:

```
http://localhost:5000
```

---

# 📡 API Endpoints (as required)

### **POST /chat**

Send user message → returns bot reply + sentiment

```
{
  "message": "hello",
  "session_id": "123"
}
```

### **GET /sentiment/conversation**

Returns final Tier-1 full conversation sentiment.

### **GET /conversation/history**

Returns all messages + per-message sentiment.

### **POST /session/new**

Starts a new chat session.

All endpoints remain unchanged as required.

---

# 🧪 Tests (Optional)

You may create:

* unit tests for SentimentAnalyzer
* endpoint tests via pytest + Flask test client

---

# ⭐ Optional Extra Credit (Already Implemented)

* Mood trend graph (backend metrics ready)
* Hybrid optimized model
* Caching for real-time performance
* Session manager with multiple conversations

---

# 📞 Support / Viva Prep

If asked in viva:

* Explain hybrid model
* Show trend + shift detection logic
* Talk about performance optimizations
* Mention modular architecture
* Explain why you used Groq API instead of Gemini

---